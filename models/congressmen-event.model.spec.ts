import { CongressmenEvent } from './congressmen-event.model';

describe('CongressmenEvent', () => {
  it('should create an instance', () => {
    expect(new CongressmenEvent()).toBeTruthy();
  });
});
